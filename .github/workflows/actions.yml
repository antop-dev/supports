name: CD

on:
  push:
    branches: [ main ]

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    name: Build
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'
          cache: gradle

      - name: Build JAR
        run: ./gradlew bootJar

      - name: Rename to app.jar
        run: mv build/libs/supports-*.jar build/libs/app.jar

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: app.jar
          path: build/libs/app.jar
          retention-days: 1

  deploy:
    name: Deploy
    needs: build
    runs-on: ubuntu-latest
    steps:
      - name: Download artifact
        uses: actions/download-artifact@v4
        with:
          name: app.jar

      - name: Upload JAR to server
        uses: appleboy/scp-action@v1.0.0
        with:
          host: ${{ secrets.SSH_HOST }}
          port: ${{ secrets.SSH_PORT }}
          username: ${{ secrets.SSH_USERNAME }}
          password: ${{ secrets.SSH_PASSWORD }}
          source: app.jar
          target: ${{ secrets.SSH_DEPLOY_DIR }}
          overwrite: true

      - name: Restart application
        uses: appleboy/ssh-action@v1.2.0
        env:
          DEPLOY_DIR       : ${{ secrets.SSH_DEPLOY_DIR }}
          DB_PATH          : ${{ secrets.APP_DB_PATH }}
          UPLOAD_DIR       : ${{ secrets.APP_UPLOAD_DIR }}
          BASE_URL         : ${{ secrets.APP_BASE_URL }}
          # Resend SMTP. 호스트/포트/사용자 이름은 고정값이라 시크릿으로 두지 않는다(RESEND.md 참고).
          MAIL_HOST        : ${{ secrets.APP_MAIL_HOST }}
          MAIL_PORT        : ${{ secrets.APP_MAIL_PORT }}
          MAIL_USERNAME    : ${{ secrets.APP_MAIL_USERNAME }}
          # Resend API 키
          MAIL_PASSWORD    : ${{ secrets.APP_MAIL_PASSWORD }}
          # 받는 사람에게 보이는 발신 주소. Resend 에서는 반드시 설정해야 한다.
          MAIL_FROM        : ${{ secrets.APP_MAIL_FROM }}
          MAIL_FROM_NAME   : ${{ secrets.APP_MAIL_FROM_NAME }}
          MAIL_REPLY_TO    : ${{ secrets.APP_MAIL_REPLY_TO }}
          SLACK_WEBHOOK_URL: ${{ secrets.APP_SLACK_WEBHOOK_URL }}
        with:
          host:     ${{ secrets.SSH_HOST }}
          port:     ${{ secrets.SSH_PORT }}
          username: ${{ secrets.SSH_USERNAME }}
          password: ${{ secrets.SSH_PASSWORD }}
          envs: DEPLOY_DIR,DB_PATH,UPLOAD_DIR,BASE_URL,MAIL_HOST,MAIL_PORT,MAIL_USERNAME,MAIL_PASSWORD,MAIL_FROM,MAIL_FROM_NAME,MAIL_REPLY_TO,SLACK_WEBHOOK_URL
          script: |
            PID_FILE="$DEPLOY_DIR/app.pid"

            # 기존 프로세스 종료
            if [ -f "$PID_FILE" ]; then
              OLD_PID=$(cat "$PID_FILE")
              if kill -0 "$OLD_PID" 2>/dev/null; then
                echo "Stopping PID $OLD_PID ..."
                kill "$OLD_PID"
                sleep 3
              fi
              rm -f "$PID_FILE"
            fi

            # 새 프로세스 시작
            nohup java -jar \
              -Xmx1024m -Xlog:gc -Djava.security.egd=file:/dev/urandom \
              -XX:+HeapDumpOnOutOfMemoryError -XX:HeapDumpPath=$DEPLOY_DIR/heapdump.hprof -XX:+ExitOnOutOfMemoryError \
              "$DEPLOY_DIR/app.jar" \
              --spring.profiles.active=prd \
              > "$DEPLOY_DIR/app.log" 2>&1 &

            echo $! > "$PID_FILE"
            echo "Started PID $(cat "$PID_FILE")"

      - name: Notify Slack
        uses: 8398a7/action-slack@v3
        if: always()
        with:
          status: ${{ job.status }}
          author_name: Gunpla Decal
        env:
          SLACK_WEBHOOK_URL: ${{ secrets.SLACK_WEBHOOK_URL }}
